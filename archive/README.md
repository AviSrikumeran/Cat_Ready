# Archive

Non-essential or reference files moved here so the main project stays clean.

- **whisper_testing/** – Manual Whisper API testing (audio + transcript). Not required for the app.
- **image1.html** / **image1_files/** – Saved webpage assets; not used by the app or tests.
- **media/** – Uploaded inspection media (audio + images per step). Used by the backend when running locally.
- **images/** – Reference/sample images (e.g. for LLM integration tests); moved here for organization.
