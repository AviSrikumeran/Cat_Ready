# Archive

Non-essential or reference files moved here so the main project stays clean.

- **whisper_testing/** – Manual Whisper API testing (audio + transcript). Not required for the app.
- **image1.html** / **image1_files/** – Saved webpage assets; not used by the app or tests. The app uses the `.jpg` images in the project root `images/` folder for LLM integration tests.
